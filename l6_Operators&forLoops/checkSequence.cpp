#include<iostream>
using namespace std;
// still working on this

int main(){
    int n;
    cin>>n;
    int i=0;
    int prev=0,curr=0, next;
    bool isDec=false, isInc=false;
    cin>>next;
    while(i<=n){
        cin>>next;
    // if dec
       if(curr<next){
        isDec=true;
       }
    // if inc
    i++;
}
}